apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-vip-setters
  annotations:
    config.kubernetes.io/local-config: "true"
    internal.kpt.dev/function-config: apply-setters
    description.kpt.dev: Sample kube-vip setters; override per cluster copy downstream
data:
  kube-vip-version: v0.8.7
  cluster-vip-address: 10.0.15.1
  cluster-vip-interface: eth0
  kube-vip-namespace: kube-vip
