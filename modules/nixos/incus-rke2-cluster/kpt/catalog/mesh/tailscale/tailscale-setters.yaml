apiVersion: v1
kind: ConfigMap
metadata:
  name: tailscale-setters
  annotations:
    config.kubernetes.io/local-config: "true"
    internal.kpt.dev/function-config: apply-setters
    description.kpt.dev: Tailscale operator sample setters for downstream overrides
data:
  tailscale-version: 1.82.0
  cluster-name: example-cluster
  vip-address: 10.0.15.1
  lb-pool-cidr: 10.0.8.0/24
  tailscale-namespace: tailscale-system
