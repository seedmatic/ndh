apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: bioskop-packagevariants
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: |
    Cluster-level composition for bioskop. This package does not ship any
    Kubernetes resources directly; it tracks which catalog packages should be
    cloned, the shared setters they consume, and the expected deployment order.
pipeline:
  mutators:
    - image: ghcr.io/kptdev/krm-functions-catalog/apply-setters:v0.2
      configPath: bioskop-setters.yaml
