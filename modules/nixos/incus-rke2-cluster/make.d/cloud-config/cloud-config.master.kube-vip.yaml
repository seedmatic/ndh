# Kube-vip overlay for highly available Kubernetes API server VIP
# Provides HA VIP management independent of Cilium LoadBalancer IPAM
# @codebase

# Kube-VIP management has been migrated to kpt package
# See: kpt-packages/ha/kube-vip/
# Deploy with: kpt live apply /var/lib/incus-rke2-cluster/kpt-packages/ha/kube-vip

write_files: []

# Original deployment (now in kpt package):
# Original Kube-VIP DaemonSet (now in kpt package):
#  - path: /var/lib/rancher/rke2/server/manifests/kube-vip.yaml
#    content: DaemonSet with VIP ${CLUSTER_VIP_GATEWAY_IP} on vmnet0
#
# Original NodePort backup (now in kpt package):
#  - path: /var/lib/rancher/rke2/server/manifests/control-plane-nodeport.yaml
