= Network Infrastructure Setup
:toc: left
:toclevels: 3
:icons: font
:source-highlighter: rouge

== Overview

The fleet's cross-network reachability rests on a *Tailscale SaaS* tailnet,
`mammoth-skate` (`mammoth-skate.ts.net`), on which the always-on machines are members. The
home LAN is `192.168.1.0/24`, served by the Bouygues *Bbox* router (`192.168.1.254`, the
catalog router `mammoth-skate`). A *self-hosted Headscale* control plane is planned as a
*workload-cluster service* (hosted on the always-live bioskop workload) — it is **not
deployed today**; the mesh in use is the Tailscale SaaS tailnet.

[NOTE]
====
This document describes the *current* model — Tart VMs, `lan-br`, `mammoth-skate.ts.net`,
the personal/corporate ownership split, and the `172.16` roaming overlay. The earlier
*Lima* / `incusbr0` / `192.168.106` / `home.arpa` model is *retired*; if you find a
reference to it elsewhere, treat it as stale.
====

Authoritative sources (this doc is a map, not the source of truth):

* *Address plan + host reservations* — `catalog/default.nix` (`netplan.*`), the single
  source of truth, reconciled against the Bbox by the `bbox-reconcile` module.
* *Module options* — link:modules-reference.adoc[Modules Reference].
* *Cluster mesh (two-plane node/workload model)* — rke2lab
  `docs/architecture/cluster-api/management-workload-topology.adoc`.
* *`vz.nikopol` roaming overlay* — link:network-topology-c4.adoc[Network Topology & vz-host
  Roaming Addressing].

== Architecture

Two planes stack (full detail in the rke2lab mesh document):

* *Node plane — Tailscale, tailnet `mammoth-skate`.* Every always-on machine is a member:
  the NixOS Incus-host VMs (`bioskop-nixos`, `nikopol-nixos`) and the personal Macs/VMs.
  Always present — the bootstrap and external-access transport.
* *Workload plane — Headscale (future).* A distinct tailnet for workloads (pods/services),
  deployed as a service on the always-live bioskop workload cluster; absent until a
  workload runs.

[source]
----
┌───────────────────────────────────────────────────────────────┐
│  Node plane — Tailscale SaaS tailnet  mammoth-skate.ts.net      │
│  members: bioskop, nikopol (personal), bioskop-nixos,          │
│           nikopol-nixos  (+ cluster nodes)                     │
│  • identity-based SSH  • MagicDNS  • cross-network reachability │
└───────────────────────────────────────────────────────────────┘
        ▲                                          ▲
        │ (future) workload plane = Headscale       │  one exception:
        │  hosted on bioskop-wrkld                   │  vz.nikopol (corp Mac)
        │                                            │  cannot join → reached
        ▼                                            ▼  locally (172.16 overlay)
   [ workloads / pods / services ]            [ see network-topology-c4.adoc ]
----

*One structural exception*: the *corporate bare-metal Mac* `vz.nikopol` (JAMF-managed)
cannot join the tailnet (no VPN binaries by policy). It is reached *locally* via a static
`172.16.6` overlay — not as a tailnet member. See
link:network-topology-c4.adoc[Network Topology & vz-host Roaming Addressing].

== Network Topology

Two bare-metal hosts, each running a NixOS Tart VM that hosts Incus. The `-nixos` VM is the
Incus host and a tailnet member; the RKE2 workload cluster runs as Incus instances on it.

=== bioskop — Mac Mini · personal · always at home

[source]
----
bioskop (Darwin/macOS, .129)                     ← personal bare-metal, own hardware
├── Tailscale (mammoth-skate member)
└── nikopol-... Tart VMs
    └── bioskop-nixos (NixOS, .130)              ← Incus host · tailnet member
        ├── lan-br (bridged on the home LAN)
        ├── advertises 192.168.1.0/24 into the tailnet (LAN subnet router)
        └── Incus: bioskop-wrkld RKE2 cluster    ← 10.80.0.0/21 (vmnet-br)
----

=== vz.nikopol — corp MacBook · JAMF-managed · roaming

[source]
----
vz.nikopol (Darwin/macOS, .1)                    ← corporate bare-metal · NOT on tailnet
│                                                  (no VPN binary by corp policy)
│   en0 (Wi-Fi) + static alias 172.16.6.253/30 (roaming-stable local overlay)
│
├── nikopol (Tart macOS VM, .33)                 ← personal env · tailnet member
│                                                  (ProxyJump hop to reach vz.nikopol)
└── nikopol-nixos (Tart NixOS VM, .34)           ← Incus host · tailnet member (cluster)
    ├── lan-br (bridged on the LAN) + .254/30 (roaming link end, ip_forward)
    ├── Incus: nikopol-wrkld RKE2 cluster        ← 10.80.8.0/21 (vmnet-br)
    └── Incus: managed 172.16.6.0/25 (dnsmasq)   ← netflow instances (collector nnh)
----

The `172.16.6` roaming overlay is *local* to this machine (not advertised); it gives the
corp Mac stable addressing to its VMs and the collector on any network. Full transport in
link:network-topology-c4.adoc[Network Topology & vz-host Roaming Addressing].

== Network Ranges

[options="header"]
|===
| Network | CIDR | Purpose
| Home LAN | 192.168.1.0/24 | main network (Bbox `mammoth-skate`, gw .254)
| Cluster supernet | 10.80.0.0/18 | RKE2 cluster network pool
| bioskop cluster | 10.80.0.0/21 | bioskop-wrkld
| nikopol cluster | 10.80.8.0/21 | nikopol-wrkld
| K8s pods | 10.42.0.0/16 | Kubernetes pods
| K8s services | 10.43.0.0/16 | Kubernetes services
| Tailnet (mammoth-skate) | 100.64.0.0/10 | Tailscale overlay
| Roaming (reserved) | 172.16.0.0/12 | per-corp-host roaming overlays
| nikopol roaming net | 172.16.6.0/25 | managed Incus net (netflow) behind nikopol-nixos
| nikopol roaming link | 172.16.6.252/30 | static P2P: vz.nikopol .253 ↔ nikopol-nixos .254
|===

Cluster CIDRs are derived arithmetically from the `10.80.0.0/18` supernet (8 × /21, one per
cluster; nodes as /23 within), by rke2lab's `ClusterNetworkBlueprint` — the source of truth
projected into this catalog via the `rke2lab` flake input. See rke2lab's netplan blueprint
docs for the allocation model.

=== Home LAN reservations (from `catalog/default.nix`)

Static reservations live in `netplan.lan.hosts` (reconciled against the Bbox). `ownership`
labels each host `personal` or `corporate` (a flow-attribution label for `nnh`; ndh
reconciles both).

[options="header"]
|===
| Host | IP | kind | ownership
| nikopol-vzhost (vz.nikopol) | 192.168.1.1 | vz-host | corporate
| zecoute / huematic / pop-screen / vertdegris / pop-cast | .5–.12 | media | personal
| nikopol | 192.168.1.33 | darwin-host | personal
| nikopol-nixos | 192.168.1.34 | nixos | personal
| nikopol-vz | 192.168.1.65 | vz-host | corporate
| bioskop | 192.168.1.129 | darwin-host | personal
| bioskop-nixos | 192.168.1.130 | nixos | personal
| bioskop-wifi | 192.168.1.158 | wifi-iface | personal
| `<cluster>-<role>` RKE2 nodes | (projected) | rke2 | personal
|===

RKE2 node reservations are a pure projection of rke2lab's blueprint (MAC scheme
`10:66:6a:4c:<clusterId>:<nodeId>`), so they stay in lockstep — no hand-copied values.

== WAN port forwards (Bbox)

From `netplan.wan.portForwards` (only ports actually configured on the Bbox):

[options="header"]
|===
| External | → Internal | Purpose
| tcp/2222 | bioskop tcp/22 | SSH into bioskop from off-LAN
| tcp/41841 | bioskop tcp/41841 | Headscale control plane (when deployed)
|===

Public IPv4 is dynamic, tracked by Duck DNS at `mammoth-skate.duckdns.org`
(`netplan.wan.ddnsHostname`).

== SSH access (current model)

* *Across networks* — via the tailnet: `ssh <host>` (MagicDNS `mammoth-skate.ts.net`) or
  `tailscale ssh <host>`. Members: `bioskop`, `nikopol`, the `-nixos` hosts.
* *`vz.nikopol`* (off-tailnet corp Mac) — `HostName 172.16.6.253`; from the personal
  `nikopol` VM it is a direct local hop, from any other host add `ProxyJump nikopol`. See
  link:network-topology-c4.adoc#usage-patterns[the roaming usage patterns].
* SSH config is rendered per host by the ndh SSH modules; see
  link:modules-reference.adoc[Modules Reference] and the catalog `netplan.tailnet.hosts`.

== Security

* All tailnet traffic is WireGuard-encrypted; access is identity-based (no shared keys).
* The corp Mac runs *no VPN binary* (policy); its reachability is a local static overlay,
  never an advertised tailnet route.
* Secrets (Bbox admin credential, tailnet auth) are sops-encrypted in `.secrets`; never
  commit plaintext.

== Related documentation

* link:network-topology-c4.adoc[Network Topology & vz-host Roaming Addressing (C4-Style UML)]
  — the local `172.16.6` overlay.
* link:nikopol-minimal-host.adoc[Corp Bare-Metal Minimal Host (vz.nikopol)] — the corp Mac +
  its Tart VMs.
* link:bioskop-deployment.adoc[bioskop Deployment Guide] — bioskop-side bring-up.
* link:modules-reference.adoc[Modules Reference] — NixOS/Darwin module options.
* rke2lab `docs/architecture/cluster-api/management-workload-topology.adoc` — the cluster
  mesh two-plane model and the off-mesh `vz.nikopol` note.
